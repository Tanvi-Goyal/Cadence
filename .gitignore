*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/
trace_processor

# Signing — the upload keystore and its credentials never enter the repo.
*.jks
*.keystore
keystore.properties

# Service/SDK config carrying live keys.
kotzilla.json

# Stray local SQLite scratch file (the app DB lives in app private storage).
/mindset.db

# Android Studio's "Generate Signed Bundle / APK" output. Not under build/, so it is not covered
# by the rule above. Holds the signed APK and a copy of mapping.txt — the R8 de-obfuscation key,
# which must never be published.
/app/release/
